parameters: 
-offer weights for change in each i/p feature

Algo:

||- got a new update for a customer. Store churn and feature values of pre and current of a customer, and offer weights in  updt.json
||- Check which feature increment improved the churn rate, and assign a weight to each feature accordingly(Remember to normalise before checking the effect). Similar update rule here, also just check which feature increases or decreases churn rate based on its increase or decrease.
||- change offer weights using update_rule(updt.json)
	- alpha(previous weight for an offer)+-(1-alpha)(offer weight used in the update)    --Here, alpha is the change in i/pf. This offer weight is for a particular feature only. - if the feature value decreases.
||- Whenever you want to give an offer for any record based on churn, just select some features based on random sampling
||-then concatenate the corresponding offer weights of each feature(while concatinating weight them based on change in them required(change required will be E[feat|Not_exited]-curfeat))
||- After getting an offer with weights, randomly sample 2-3 and pass it to LLM and ask it to make an offer.

- ?How will the end offer be generated?-Store updates. It will be helpful for LLM to produce a precise offer based on its previous decisions. Update.csv is its KB. How is it helping in shaping a new offer?

-How Bank loss will be quantified

Things to remember:
No need to keep track of action separately for all customers
All customers are equal who have the same update and the same record no matter what their past offer history is before t-1.
Weight parameters are used for the influence
Weights can be any positive number.
Don't consider gender. Gender based offer twik will be done by LLM.
Offers are just for increasing feature value
Offer generated by LLM will be meaningful as it is provided its history of offer generated based on what input it was provided
Everything is in weights and whenever Probability is required Softmax is applied
There is no offer for reducing something. So, no need to worry for negatives
Here there is constant search for correct value of weights of any offer

x is a vector, y=1 exists and 0 not exited

Initiate_weights_of_feature(ori.csv)
{
	D[x]=E[x|y=0]-E[x|y=1]
	Min-Max_Standardize(D[x])
	//Check overall which feature increases churn or decreases
	for i in D
	{
		Assign each to the corresponding feature weight.
	}
}

Initialize_weights_of_offer()
{
	Assign 0.5 to the offer_weights of all features
}

add_to_csv(pre_rec,cur_rec,pre_churn,current_churn,offer_given,Offer_string)
{
	-----
}

Store_in_observation(cust_id)
{
	#if customer don't use offer at the end, then resample offers code (i), but this time select diiferent offer.
	pass it to LLM and ask to make new offer for the customer
	LLM(Offers_importance,current_rec,churn_rate,Update_csv[Offer_given],Offer_string,credit_score,previous_one_rejected=1)
	continue the observation
}

Update_offer_weights(pre_rec,cur_rec,pre_churn,current_churn,offer_given,Offer_string)
{
	D_rec=cur_rec-pre_rec
	D_churn=cur_c-pre_c
	Min-Max_Standardize(D_rec|D[x])
	for i in D_rec
	{
		for(j in is_given(offer,offer_given))
		{
			offer[i,j] = (1-alpha)*offer[i,j] +- alpha*offer_given[j] #These are weights for getting probability apply softmax
		}
	}
}

give_offer(cur_rec,churn_rate)
{
	D_rec_req=E[x|y=0]-cur_rec #Scalarize

	Randomly select 2-3 features on which you need to make an offer. Select based on the difference.
	
	Offer_weight=offers[i,:]*D_rec_req[i]+offers[j,:]*Dec_rec_req[j]+offers[k,:]*Dec_rec_req[k] # i,j,k th features were selected
	Offers_importance=select_any_2(softmax(Offer_weight)) -----(i)
	LLM(Offers_importance,current_rec,churn_rate,Update_csv[Offer_given],Offer_string,credit_score,previous_one_rejected=0) # adjusting perks like time and money based on importance given to each offer element and the credit score of the person
	Store_in_observation(cust_id)
	add_to_csv(remove_from_observation(cust_id))# After considerable time add to update.csv with new records
	added_non_updated_count++
	if(added_non_updated_count>20)
	{
		Update_offer_weights(pre_rec,cur_rec,pre_churn,current_churn,offer_given,Offer_string)
	}
}















